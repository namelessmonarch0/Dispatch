/target
**/*.rs.bk
*.pdb

# Vendored Zig build artifacts
vendor/libghostty-vt/zig-out/
vendor/libghostty-vt/.zig-cache/

# Snapshot test rejects
**/*.snap.new

.DS_Store

# Zig fetches dependencies into the vendored tree on first build
vendor/libghostty-vt/zig-pkg/
